config {
  type: "declaration",
  database: dataform.projectConfig.defaultDatabase,
  schema: dataform.projectConfig.vars.env === "prod" ? "claims_raw" : "dev_claims_raw",
  name: "raw_claims",
  description: "Raw claims data loaded from GCS CSV."
}
